const home = {
    name: 'Home',
    path: '/'
}

const shop = {
    name: 'Shop',
    path: '/shop'
}

const favorites = {
    name: 'Favorites',
    path: '/favorites'
}

const checkout = {
    name: 'Checkout',
    path: '/checkout'
}

const ROUTES = {
    home,
    shop,
    favorites,
    checkout
};

export default ROUTES;